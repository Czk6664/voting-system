# 投票系统部署说明

## 📚 文档导航

我已经为你创建了完整的部署文档和工具，按照以下顺序阅读：

### 1. 快速开始（5分钟）
👉 **[QUICKSTART.md](./QUICKSTART.md)** - 最快的部署方式
- 一键部署脚本
- Docker 快速部署
- 常用命令速查

### 2. 完整部署指南（详细）
👉 **[DEPLOYMENT.md](./DEPLOYMENT.md)** - 详细的部署文档
- 三种部署方式详解（PM2/systemd/Docker）
- Nginx 反向代理配置
- HTTPS 证书配置
- 安全建议
- 性能优化
- 故障排查

### 3. 部署检查清单
👉 **[DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)** - 部署前后检查
- 部署前准备
- 部署步骤
- 功能测试
- 安全检查

---

## 🚀 三种部署方式

### 方式一：一键部署（最简单）

```bash
# 1. 上传代码到服务器
scp -r ./* user@your-server:/path/to/app/

# 2. 登录服务器并运行
ssh user@your-server
cd /path/to/app/
chmod +x deploy.sh
./deploy.sh
```

✅ 自动安装依赖、启动应用、配置开机自启

---

### 方式二：Docker 部署（推荐）

```bash
# 1. 上传代码到服务器
scp -r ./* user@your-server:/path/to/app/

# 2. 启动容器
cd /path/to/app/
docker-compose up -d
```

✅ 环境隔离、快速部署、易于管理

---

### 方式三：手动部署（完全控制）

```bash
# 1. 安装依赖
npm install

# 2. 安装 PM2
sudo npm install -g pm2

# 3. 启动应用
pm2 start ecosystem.config.js

# 4. 保存配置
pm2 save

# 5. 设置开机自启
pm2 startup
```

✅ 完全控制、灵活配置

---

## 📁 部署相关文件说明

### 配置文件
- **`.env.example`** - 环境变量模板（复制为 `.env` 并修改）
- **`ecosystem.config.js`** - PM2 配置文件
- **`docker-compose.yml`** - Docker Compose 配置
- **`Dockerfile`** - Docker 镜像配置
- **`nginx.conf.example`** - Nginx 配置示例

### 脚本文件
- **`deploy.sh`** - 一键部署脚本（自动化部署）
- **`package.json`** - 项目依赖和启动脚本

### 文档文件
- **`QUICKSTART.md`** - 快速开始指南
- **`DEPLOYMENT.md`** - 完整部署文档
- **`DEPLOYMENT_CHECKLIST.md`** - 部署检查清单
- **`README.md`** - 项目说明

---

## ⚙️ 部署前准备

### 1. 修改管理员密码

复制环境变量模板：
```bash
cp .env.example .env
```

编辑 `.env` 文件：
```bash
nano .env
```

修改密码：
```
ADMIN_PASSWORD=your_strong_password_here
```

### 2. 检查服务器要求

- ✅ Node.js 14+ 已安装
- ✅ 至少 512MB 内存
- ✅ 端口 3000 可用
- ✅ 防火墙已配置

---

## 🌐 配置域名（可选）

### 1. 安装 Nginx
```bash
sudo apt-get install nginx
```

### 2. 配置 Nginx
```bash
# 复制配置文件
sudo cp nginx.conf.example /etc/nginx/sites-available/voting-system

# 修改域名
sudo nano /etc/nginx/sites-available/voting-system
# 将 your-domain.com 改为你的域名

# 启用配置
sudo ln -s /etc/nginx/sites-available/voting-system /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 3. 配置 HTTPS（免费）
```bash
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 📊 部署后访问

部署完成后，通过以下地址访问：

- **主页**：http://your-server:3000
- **管理员**：http://your-server:3000/admin.html
- **投票页**：http://your-server:3000/voter.html
- **展示页**：http://your-server:3000/display.html

如果配置了域名：
- **主页**：https://your-domain.com
- **管理员**：https://your-domain.com/admin.html
- **投票页**：https://your-domain.com/voter.html
- **展示页**：https://your-domain.com/display.html

---

## 🔧 常用命令

### PM2 命令
```bash
pm2 status              # 查看状态
pm2 logs voting-system  # 查看日志
pm2 restart voting-system  # 重启
pm2 stop voting-system  # 停止
pm2 monit              # 监控
```

### Docker 命令
```bash
docker-compose ps       # 查看状态
docker-compose logs -f  # 查看日志
docker-compose restart  # 重启
docker-compose down     # 停止
```

### Nginx 命令
```bash
sudo nginx -t           # 测试配置
sudo systemctl restart nginx  # 重启
sudo systemctl status nginx   # 查看状态
```

---

## 🛡️ 安全建议

1. ✅ **修改默认密码**：使用强密码（至少12位）
2. ✅ **启用 HTTPS**：使用 Let's Encrypt 免费证书
3. ✅ **配置防火墙**：只开放必要端口
4. ✅ **定期更新**：`npm update` 更新依赖
5. ✅ **定期备份**：备份重要数据
6. ✅ **限制访问**：使用 IP 白名单限制管理员页面

---

## ❓ 常见问题

### Q: 端口被占用怎么办？
A: 修改 `.env` 文件中的 `PORT` 为其他端口（如 8080）

### Q: 忘记管理员密码怎么办？
A: 修改 `.env` 文件中的 `ADMIN_PASSWORD`，然后重启应用

### Q: WebSocket 连接失败？
A: 检查防火墙设置和 Nginx 配置中的 WebSocket 支持

### Q: 如何查看日志？
A: 
- PM2: `pm2 logs voting-system`
- Docker: `docker-compose logs -f`
- systemd: `sudo journalctl -u voting-system -f`

### Q: 如何备份数据？
A: 当前使用内存存储，重启会丢失数据。建议：
1. 定期导出 JSON 数据
2. 或修改代码使用数据库（MongoDB/PostgreSQL）

---

## 📞 需要帮助？

1. 查看详细文档：[DEPLOYMENT.md](./DEPLOYMENT.md)
2. 查看快速指南：[QUICKSTART.md](./QUICKSTART.md)
3. 查看检查清单：[DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)
4. 查看应用日志排查问题

---

## 🎉 部署成功！

如果一切顺利，你的投票系统现在应该已经在服务器上运行了！

**下一步：**
1. 登录管理员页面
2. 修改默认密码
3. 创建第一个投票
4. 开始使用系统

祝你使用愉快！🎊
